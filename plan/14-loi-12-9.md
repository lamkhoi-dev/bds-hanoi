# Plan: Sửa & bổ sung theo "lỗi 12-9" (2 site BĐS, chung mã nguồn)

> Bước 0 khi bắt đầu làm: chép file này vào `bds-hanoi/plan/14-loi-12-9.md` (quy tắc plan của anh).

## Context

Khách gửi `C:\Users\ADMIN\Downloads\lỗi 12-9.docx` gồm 9 mục. Mục 1 khách đã gạch ngang (xong).
Còn **8 việc**: 5 lỗi, 2 chỉnh nhỏ, và 1 nâng cấp lớn (quản trị tin tức).

Đã điều tra trên code, log máy chủ và CSDL thật (chỉ đọc). **Mọi lỗi đều đã tìm ra gốc**, không có
lỗi nào phải đoán:

| # | Khách báo | Gốc lỗi (đã chứng minh) |
|---|---|---|
| 2 | Trang khu vực "thỉnh thoảng lấy tin khu vực khác" | Trang khu vực lọc tin **theo tên chữ**, không theo mã khu vực. 35 tên xã/phường trùng giữa các huyện/tỉnh. Đo: `/ha-huy-tap-ha-tinh-2` (Hà Tĩnh) hiện 6 tin — **cả 6 là tin TP Vinh**; `/nghi-phong-nghi-loc` 3/3 tin Vinh; `/nghia-thai-tan-ky` 1/1 tin Nam Đàn. Thêm: khối "Tin đăng mới nhất" (thêm 05/09) hiện tin toàn site trên trang rỗng mà không ghi là tin nơi khác. |
| 3 | Lọc giá "vẫn lấy dữ liệu ngoài khoảng" | 3 nguyên nhân: (a) so sánh chạm biên — tin chỉ có nhãn "2–3 tỷ" lọt vào "1–2 tỷ"; (b) giá cụ thể và nhãn khoảng lệch nhau do người đăng tự chọn (2 tỷ: 1 tin nhãn "1–2", 3 tin nhãn "2–3"); (c) lọc "Dưới 500 triệu" ở /search lẫn tin **cho thuê** 7 triệu/tháng. Đo: 72 tin lệch nhãn (/search), 60 (/seo). Diện tích dính y hệt (25 tin lệch). Kèm: "Thỏa thuận" trả về mọi tin có giá; link trang chủ `LT_1B` là mã không tồn tại; tin VIP bị trả về 2 lần. |
| 4 | Đăng nhập số điện thoại "lỗi fiber" | = lỗi **Firebase**. Khi chuyển site sang `/app-new` (15/08), 6 biến `NEXT_PUBLIC_FIREBASE_*` bị rơi → bản chạy dùng `YOUR_API_KEY` (đã thấy trong bundle). Cấu hình thật còn ở `/app/frontend/.env`, **cùng dự án** `nha-dat-xu-nghe-8504d` với khoá backend; Firebase đang cho phép `nhadatxunghe.vn` + `www`. Kèm lỗi code: số `84…` bị nhân đôi `+8484…`; đăng nhập OTP bằng `+84…` không khớp tài khoản lưu `0…` → **tạo tài khoản trùng**. |
| 5 | Không đăng được tin tức trên PC | Log 07/09: **15 lần** `PayloadTooLargeError` tại `/api/v1/news` (18:28→21:03). Máy chủ chỉ nhận 100 KB; trình soạn thảo dán ảnh chụp/Word thành dữ liệu nhúng base64 trong nội dung. |
| 7 | Link người đăng quá dài | `/user/{tên}-{uuid 36 ký tự}`. Bảng User không có mã ngắn (tin đăng thì có `shortCode`). |
| 8 | Kiểm duyệt tin: phân cách hàng nghìn | `ReviewModal` dùng ô `type="number"` thô. |
| 6 | Nâng cấp quản trị tin tức | Hiện chỉ có tiêu đề/ảnh/nội dung; trình soạn thảo tự viết (execCommand), không có trạng thái, chuyên mục, tác giả, SEO; không làm sạch HTML; lớp `prose` không có tác dụng (chưa cài plugin). |

**Anh đã chốt:**
- Giá đúng bằng mốc (vd 2 tỷ) → **hiện ở cả 2 khoảng** "1–2" và "2–3" (diện tích tương tự).
- Giá cụ thể và khoảng lệch nhau → **tin giá cụ thể** (khoảng tự chọn theo giá; sửa lại tin cũ).
- Tin tức: **chỉ ADMIN** đăng/sửa/xoá; ẩn menu Tin tức với MOD.
- OTP Hà Nội: **khách tạo Firebase riêng** → trong lúc chờ, tự ẩn nút OTP trên Hà Nội.

Mã nguồn chung ⇒ mọi bản sửa áp cho cả 2 site. Deploy luôn **Hà Nội (staging) trước, Nghệ An sau**.

---

## PHẦN A — Sửa lỗi (1 đợt deploy, có 1 migration nhỏ)

### A1. Đăng nhập OTP (mục 4)
- **Vận hành Nghệ An:** sao lưu `/app-new/.env`, chép 6 biến `NEXT_PUBLIC_FIREBASE_*` từ `/app/frontend/.env`, build lại frontend.
- `frontend/src/lib/firebase.ts`: bỏ giá trị giả, xuất `isFirebaseConfigured`.
- `frontend/src/app/login/page.tsx`: ẩn tab OTP khi chưa cấu hình (Hà Nội); nhãn ô đăng nhập "Email hoặc số điện thoại"; đổi lỗi Firebase (`auth/invalid-phone-number`, `too-many-requests`, `invalid-verification-code`, `quota-exceeded`…) thành câu tiếng Việt, không hiện chữ "Firebase: Error".
- Mới `frontend/src/lib/phone.ts` `normalizeVnPhone` (0 / 84 / +84, dấu cách, dấu chấm → `+84…`) dùng trong `services/sms/FirebaseSmsProvider.ts`; reset reCAPTCHA khi đổi bước/đổi tab.
- Backend `auth.service.ts` (`validateUser`, `verifyPhoneFirebase`) + `user.service.ts` tra cứu khớp **cả** `0xxxxxxxxx` và `+84xxxxxxxxx` → không tạo tài khoản trùng.
- **Kiểm chứng:** bundle Nghệ An hết `YOUR_API_KEY`; trang đăng nhập không còn lỗi `invalid-api-key`; spec `phone.spec.ts`; Hà Nội không hiện tab OTP. Gửi SMS thật cần anh/khách thử 1 lần bằng số thật.

### A2. Trang khu vực lẫn tin (mục 2)
- `backend/src/property/property.service.ts` `getSeoProperties` (~L1917-1946) + `property-utils.ts` `buildPrismaWhere`: lọc **theo mã**:
  - Tỉnh → `provinceId`; Huyện → `districtId`
  - Xã mới → `wardId = id` **hoặc** (chưa có mã **và** `districtId = huyện cha` **và** tên khớp chính xác)
  - Xã cũ → `oldWardId = id` **hoặc** (chưa có mã **và** `districtId = huyện cha` **và** tên khớp)
  (40 tin xã cũ nhập tay chưa có mã vẫn hiện đúng huyện của nó.)
- Trang không có tin: backend trả thêm `nearby` = tin của **khu vực cha** (xã → huyện, huyện → tỉnh), **cùng loại giao dịch** (trang thuê hiện tin thuê). `frontend/src/app/[...slug]/page.tsx` ghi rõ: *"Chưa có tin đăng tại {Xã X}. Tin đăng gần đó tại {Huyện Y}"*. Khi gọi API lỗi: khối ghi *"Tin đăng khác bạn có thể quan tâm"* — không bao giờ để tin nơi khác hiện như tin của khu vực.
- `frontend/src/components/SidebarFilter.tsx`: tỉnh lấy theo huyện (Hà Tĩnh không bị gán "Nghệ An" → hết 0 kết quả); xã cũ gửi đúng tham số `oldWard`.
- **Kiểm chứng** (đo lại trên site thật):

  | Trang | Trước | Sau |
  |---|---|---|
  | `/ha-huy-tap-ha-tinh-2` | 6 tin Vinh | 0 + khối "gần đó tại TP Hà Tĩnh" |
  | `/nghi-phong-nghi-loc` | 3 tin Vinh | 0 + khối "gần đó tại Huyện Nghi Lộc" |
  | `/nghia-thai-tan-ky` | 1 tin Nam Đàn | 0 + khối gần đó |
  | `/ha-huy-tap`, `/nghi-phong`, `/thanh-pho-vinh` | 6 / 3 / 139 | **không đổi** |

### A3. Bộ lọc giá & diện tích (mục 3)
- `backend/src/property/property-utils.ts` (`normalizeSearchFilters`, `rangeWhere`, `meiliRangeClause`, `applyRangeKeys`):
  - Lọc theo khoảng K = [min, max]: tin khớp khi **(có giá cụ thể và min ≤ giá ≤ max)** hoặc **(không có giá và nhãn = K)**. → 2 tỷ hiện ở cả 2 khoảng; tin chỉ có nhãn không lọt sang khoảng bên.
  - Tìm mã khoảng ở cả bảng bán lẫn thuê; khoảng bán ⇒ chỉ tin bán, khoảng thuê ⇒ chỉ tin thuê.
  - "Thỏa thuận" ⇒ chỉ tin thỏa thuận. Mã không tồn tại ⇒ bỏ qua (không lọc bừa).
  - Khoảng tuỳ chỉnh (`gia=`): giá cụ thể trong khoảng, hoặc khoảng của tin nằm **trọn** trong khoảng lọc.
  - Diện tích làm y hệt.
  - Khi lưu tin (tạo, sửa, nháp, **kiểm duyệt** `property-review.service.ts`): có giá cụ thể ⇒ tự đặt nhãn khoảng theo giá. Kiểm duyệt xong **cập nhật lại Meilisearch** (hiện đang không làm).
- Frontend: `app/post/page.tsx` + `components/admin/ReviewModal.tsx` — gõ giá/diện tích cụ thể thì ô khoảng tự chọn theo (helper `rangeKeyForValue` trong `constants/ranges.ts`, backend giữ bản song song + spec đối chiếu).
- `app/page.tsx:255` sửa link `LT_1B` → `?transactionType=BAN&gia=duoi-1-ty`; `/search` bỏ trùng VIP ở cả `ups` lẫn `normals`.
- Mới `backend/src/scripts/backfill-range-keys.ts` (chạy thử → `--apply`): tính lại nhãn + biên cho tin cũ theo giá/diện tích cụ thể; rồi `node dist/scripts/reindex-search.js`.
- **Kiểm chứng:** chạy lại 2 script đo (`chk2.py`, `chkarea2.py` trong scratchpad) trên `/properties/search` và `/properties/seo` ⇒ **0 tin lệch**, 0 tin trùng; spec cho các ca: 2 tỷ ở 2 khoảng, nhãn-không-giá không lọt, tin thuê không vào khoảng bán, Thỏa thuận, mã lạ.

### A4. Link người đăng ngắn (mục 7)
- Migration `20260914100000_user_short_code`: sequence + cột `User.shortCode` mặc định `bds_to_base36(nextval(...))` (hàm đã có từ migration tin đăng), backfill, unique. Prisma `@default(dbgenerated(...))` ⇒ mọi đường tạo tài khoản (đăng ký, OTP, Google, admin) tự có mã. Cập nhật `scripts/preflight-check.ts`.
- Backend `user/user.controller.ts` + `user.service.ts getPublicProfile`: nhận mã ngắn (đoạn cuối) hoặc UUID cũ; `property.service.ts` (~L1241) select thêm `shortCode`.
- Frontend `lib/seo/canonical.ts`: `userProfilePath(user)` → `/user/{tên}-{mã}` + `parseUserRef` (spec). Thay 3 chỗ dựng link: `PropertyDetailClient.tsx:597`, `PropertyContactBox.tsx:34`, `admin/requirements/page.tsx:213`.
- `app/user/[slug]/page.tsx`: canonical về link mới, **301** từ link UUID cũ. `proxy.ts` công khai thêm dạng mã ngắn, loại trừ các trang riêng tư (`/user/settings`, `/user/properties`…); cập nhật `proxy-rules.spec.ts`.
- **Kiểm chứng:** link trên trang tin dạng ngắn → 200; link UUID cũ → 301 sang link mới; `/user/settings` khi chưa đăng nhập vẫn chuyển về đăng nhập; 100% user có `shortCode`.

### A5. Phân cách hàng nghìn (mục 8)
- Mới `frontend/src/lib/number-format.ts` (định dạng/đọc lại, có spec) + `components/NumberInput.tsx` (hiện `1.400.000.000`, gửi số).
- Áp vào `ReviewModal` (giá, diện tích có phần thập phân), `admin/settings` (giá VIP/UP), `admin/users` (nạp tiền); `admin/requirements` hiển thị giá có dấu chấm; lịch sử sửa tin (`property-review.service.ts:74`) định dạng cả giá trị cũ.
- **Kiểm chứng:** mở Kiểm duyệt tin thấy `1.400.000.000`; lưu vào CSDL vẫn `1400000000`.

### A6. Chữa cháy đăng tin tức trên PC (mục 5)
- `backend/src/main.ts`: nâng giới hạn JSON lên 2 MB.
- `components/SimpleEditor.tsx`: dán/kéo ảnh → tải lên `/upload/image` rồi chèn link (không nhúng base64); gỡ ảnh `data:`/`file:` khỏi nội dung dán từ Word; thêm nút "Chèn ảnh".
- Trang tạo/sửa tin: hiện đúng lý do lỗi từ máy chủ (413 → "Nội dung quá lớn, hãy dùng nút Chèn ảnh").
- **Kiểm chứng:** gửi 1,5 MB không đăng nhập ⇒ 401 (không còn 413); dán ảnh chụp trên PC ⇒ đăng thành công; log hết `PayloadTooLarge`.

**Deploy Phần A:** pg_dump → migration A4 → build frontend + backend → backfill A3 → reindex → đo lại bảng A2/A3 trên cả 2 site.

---

## PHẦN B — Nâng cấp quản trị tin tức (mục 6) — 5 đợt deploy, mỗi đợt dùng được ngay

### B1. CSDL, API, làm sạch HTML, trạng thái & hẹn giờ
- Migration `20260914200000_news_editorial_upgrade` (chỉ THÊM): enum `NewsStatus {DRAFT, PUBLISHED, HIDDEN}`; bảng `NewsCategory`; `News` thêm `sapo`, `thumbnailAlt/Caption/Credit/Width/Height`, `status` (mặc định PUBLISHED), `publishedAt` (backfill = `createdAt`), `contentUpdatedAt`, `authorName`, `categoryId`, `sources` (JSON — không đặt tên `references`, từ khoá SQL), `seoTitle`, `metaDescription`, `canonicalUrl`, `relatedPropertyIds`; index `(status, publishedAt)`.
- `backend/src/news/`: DTO có kiểm tra (theo mẫu `project/dto/project.dto.ts`); `news-html.ts` làm sạch bằng `sanitize-html` (danh sách thẻ khớp đúng thứ trình soạn thảo xuất ra, bỏ `data:`); `news-visibility.ts` — công khai chỉ `PUBLISHED` và `publishedAt ≤ now` (hẹn giờ không cần cron); route quản trị tách riêng `/news/admin/all`, `/news/admin/:id`; `news-categories` controller riêng; gọi `SeoService.invalidate()` khi ghi. Giữ nguyên logic slug + `previousSlugs` (301). Chỉ ADMIN ghi.
- Script `sanitize-news-content.ts`: sao lưu JSON → chạy thử (độ dài chữ trước/sau phải bằng nhau) → `--apply`.
- `seo.service.ts`: sitemap tin chỉ bài đã đăng, `lastmod = contentUpdatedAt ?? publishedAt`.
- **Kiểm chứng:** số bài trên `/news` không đổi; form cũ vẫn đăng được; bài Nháp → 404 và không có trong sitemap.

### B2. Trình soạn thảo mới (TipTap v3)
- `frontend/src/components/news-editor/`: đậm, nghiêng, gạch chân, H2/H3, căn lề, màu chữ, link, danh sách, trích dẫn, **bảng**, **ảnh kèm alt + chú thích** (node `figure` tự viết); dán/kéo ảnh tự tải lên; dọn rác khi dán từ Word (`lib/news/paste-cleanup.ts`). Link nội bộ không bị gắn `nofollow`.
- Chỉ tải ở trang quản trị (`dynamic`, `ssr:false`), ghim cùng 1 phiên bản mọi gói `@tiptap/*`; bỏ gói `react-quill` không dùng. Dự án (du-an) vẫn giữ trình soạn thảo cũ.
- `styles/news-content.css` định dạng H2/H3, danh sách, trích dẫn, bảng, ảnh cho cả trang soạn lẫn trang công khai.
- `upload.controller.ts` trả thêm kích thước ảnh.
- **Kiểm chứng:** lưu → mở lại → HTML giống hệt; dán Word/Google Docs/ảnh chụp không sinh `data:`; dung lượng JS trang công khai không tăng.

### B3. Form quản trị tin tức
- Form dùng chung `components/admin/news/NewsForm.tsx`: tiêu đề + sapo · trình soạn thảo · ảnh đại diện (gợi ý **tối thiểu 1200×675**, cảnh báo nếu nhỏ hơn, alt/chú thích/nguồn ảnh) · trạng thái Nháp/Xuất bản/Ẩn + ngày giờ đăng (hẹn giờ) · chuyên mục (+ trang `admin/news/categories`) · tác giả (điền sẵn tên người đang đăng nhập) · nguồn tham khảo · SEO tuỳ chọn (SEO title, meta description, canonical, bộ đếm ký tự, xem trước Google) · BĐS liên quan (dán link/mã tin).
- Danh sách: nhãn trạng thái (Nháp / Đã đăng / Hẹn giờ / Ẩn), lọc theo trạng thái. Trang xem trước chỉ trong quản trị. Ẩn menu Tin tức với MOD (`admin/layout.tsx`).
- **Kiểm chứng:** đăng nháp / hẹn giờ 10 phút / đăng ngay đều đúng; lỗi hiện tiếng Việt.

### B4. Trang bài viết công khai — SEO
- `lib/seo/schema/news-article.ts` (NewsArticle: tác giả, ngày đăng, ngày cập nhật, ảnh 1200×675); `lib/news/breadcrumb.ts` (Trang chủ › Tin tức › Chuyên mục › Bài); `lib/news/seo.ts` (title/description/canonical có dự phòng); `lib/news/dates.ts` (giờ Việt Nam — hiện đang lệch 7 tiếng).
- `app/news/[slug]/page.tsx`: hiện sapo, tác giả, ngày đăng, ngày cập nhật, chú thích + nguồn ảnh, nguồn tham khảo; Open Graph kiểu article + Twitter card riêng từng bài; canonical.
- **Kiểm chứng:** Google Rich Results Test đạt NewsArticle + BreadcrumbList; Facebook Sharing Debugger hiện đúng ảnh/tiêu đề; bài đăng 23:30 hiện đúng ngày.

### B5. Bài liên quan, BĐS liên quan, trang chuyên mục, sitemap
- `GET /news/:id/related`: 4 bài cùng chuyên mục; BĐS = chọn tay trước, bù tin VIP/UP còn hạn, rồi tin mới.
- `app/news/chuyen-muc/[slug]/page.tsx` + phân trang `/news`; sitemap thêm chuyên mục có bài.
- **Kiểm chứng:** khối liên quan hiện đúng; chuyên mục rỗng không vào sitemap.

---

## Test tự động (chạy trước mỗi đợt deploy)
Backend jest: `property-utils` (lọc khoảng + lọc khu vực theo mã), `news-html`, `news.service` (trạng thái, hiển thị, slug cũ vẫn đạt), DTO, `seo.service`. Frontend jest (`src/lib`): `phone`, `number-format`, `canonical` (link user), `ranges`, `news-article`, `breadcrumb`, `dates`, `paste-cleanup`. Toàn bộ test hiện có (144 + 108) phải xanh.

## Thứ tự làm
A (cả 6 việc, 1 đợt) → B1 → B2 → B3 → B4 → B5. Mỗi đợt: Hà Nội trước → đo → Nghệ An → đo lại.

## Chờ khách / cần anh
- **Hà Nội — Firebase riêng:** khách tạo dự án, gửi 6 giá trị cấu hình web + khoá service account, thêm tên miền `sanbdshanoi.vn`.
- **Thử OTP thật 1 lần trên Nghệ An** sau A1 (cần số điện thoại thật nhận SMS).
- Vẫn còn từ trước: logo Hà Nội, SMTP, tài khoản Google, DNS, xác nhận SePay, tài khoản thử lưu tin/tin đã xem.

---

## KẾT QUẢ — 14/09/2026, PHẦN A đã deploy cả 2 site, kiểm chứng trên dữ liệu thật

### A2 — khu vực trùng tên
```
/ha-huy-tap-ha-tinh-2   total=0  (trước: 6 tin, cả 6 là tin TP Vinh)  -> nearby: Thành phố Hà Tĩnh (3 tin)
/nghi-phong-nghi-loc    total=0  (trước: 3 tin Vinh)                  -> nearby: Huyện Nghi Lộc (3 tin)
/nghia-thai-tan-ky      total=0  (trước: 1 tin Nam Đàn)               -> nearby: Huyện Tân Kỳ (1 tin)
/ha-huy-tap, /nghi-phong, /thanh-pho-vinh: không đổi (6, 3, 139) — đúng khu vực của chúng.
```

### A3 — lọc giá/diện tích
Backfill: 27/228 tin lệch nhãn khoảng đã sửa (bắt được cả 1 tin dữ liệu test/rác).
Kiểm lại toàn bộ 9 khoảng giá × 10 khoảng diện tích trên `/properties/search`:
**461 lượt kiểm, 0 tin lệch thật.** 21 ca "ở đúng biên" (đúng mốc 2 tỷ/3 tỷ...) hiện ở
cả 2 khoảng liền kề — đúng quyết định đã chốt, không phải lỗi.
"Thỏa thuận": 14 tin, 0 tin không-thỏa-thuận lọt vào.

### A1 — đăng nhập
Bundle Nghệ An hết `YOUR_API_KEY`. Payload 1,5 MB tới `/api/v1/news` trả 401 (không còn 413).

### A4 — link người đăng
`/user/thuy-hien-256o` (4 ký tự, trước là UUID 36 ký tự) → 200.
**Bắt được 1 lỗi khi kiểm chứng trên site thật** (không lộ ra ở test đơn vị): link với
tên sai nhưng shortCode đúng đáng lẽ 301 sang tên đúng thì lại trả 404 — do
`permanentRedirect()` bị khối `catch` bao trùm cả hàm nuốt mất. Đã sửa + deploy lại,
đo lại ra đúng 308.

### Migration
`20260914100000_user_short_code` chạy sạch cả 2 site. Nghệ An: 44/44 user có mã, 228 tin
nguyên vẹn. Hà Nội: 1/1 user có mã. Phát hiện thêm: `20260823000000_hot_area` (từ phiên
trước) chưa từng chạy trên Nghệ An — đã áp cùng đợt này.

### Không hỏng gì
Toàn bộ trang chính cả 2 site: 200. Test: backend 187 xanh, frontend 145 xanh.

## Còn lại
PHẦN B (nâng cấp quản trị tin tức — 5 đợt) chưa làm, xem plan gốc phía trên.
